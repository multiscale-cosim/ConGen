from distutils.core import setup

# TODO: autogenerate this list using CMake
setup(name='vicogen',
      version='${PACKAGE_VERSION}',
      package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}' },
	  # List the subdirectories to be include in the build 
      packages=['vicogen',
				'vicogen.neuroml',
				])
				
